<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="script.piccap" name="Piccap Backlight Toggle" version="1.2.0" provider-name="Bryan">
  <requires>
    <import addon="xbmc.python" version="3.0.0"/>
  </requires>
  <extension point="xbmc.python.script" library="default.py">
    <provides>executable</provides>
  </extension>
  <extension point="xbmc.addon.metadata">
    <summary lang="en">Toggle Piccap ambient backlight on LG WebOS TVs</summary>
    <description lang="en">Controls the Piccap screen capture service on rooted LG WebOS TVs for use with HyperHDR/Hyperion ambient lighting.

Includes a guided setup wizard that automatically discovers your TV, generates SSH keys, and configures the connection.

Requirements:
- Rooted LG WebOS TV with Piccap installed
- SSH access enabled on the TV

Getting started:
1. Run the add-on — the setup wizard launches automatically on first use
2. Or open Settings &gt; Setup &gt; Run Setup Wizard at any time
3. Optional: Map to a remote button via keymap

Keymap example (save to userdata/keymaps/piccap.xml):
&lt;keymap&gt;&lt;global&gt;&lt;remote&gt;
  &lt;red&gt;RunScript(script.piccap)&lt;/red&gt;
&lt;/remote&gt;&lt;/global&gt;&lt;/keymap&gt;
    </description>
    <platform>all</platform>
    <license>MIT</license>
    <website>https://github.com/webosbrew/piccap</website>
  </extension>
</addon>
